
mavenJob('example') {
    logRotator(-1, 10)
    jdk('Java 8')
    scm {
        github('https://github.com/cekomc/aplatapus-framework.git', 'master')
    }
    triggers {
        githubPush()
    }
    goals('clean test')
}